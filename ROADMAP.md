# ZPatchIG Roadmap

## v0.1
- read-only app-local image discovery;
- Legacy AP / C4M0 family classification;
- 0xA0 descriptor ABI locator;
- C4M0 `loadConfig:` IMP owner evidence;
- analysis JSON and diagnostics;
- no hook installation or memory patch execution.

## Next
- descriptor producer/consumer observation under bounded windows;
- legacy secret-wrapper decoding only after current samples revalidate the fingerprint;
- canonical static patch export only after UUID/address/original-byte truth gates pass.
