name: HFAMap v2.4.1 Dispatcher Patch Args

on:
  push:
    branches:
      - feature/hfamap-v2.4.1-dispatcher-patch-args
    paths:
      - 'hfamap/**'
      - 'tests/**'
      - 'tools/**'
      - '.github/workflows/theos-hfamap-v2-bounded-universal.yml'
  workflow_dispatch:

jobs:
  test-and-build:
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Run host tests
        run: |
          set -euo pipefail
          python3 -m unittest discover -s tests -p 'test_*.py' -v
          python3 -m py_compile tools/*.py tests/*.py

      - name: Check source invariants
        run: |
          set -euo pipefail
          grep -q 'kHFAMaxImages = 2048' hfamap/src/HFAMapImageProbe.mm
          grep -q 'com.hfa.menu-binary-evidence/v2' hfamap/src/HFAMapImageProbe.mm
          grep -q '@"menuFamily"' hfamap/src/HFAMapImageProbe.mm
          grep -q '@"patchBackends"' hfamap/src/HFAMapImageProbe.mm
          ! grep -q 'family = @"memorypatch-menu"' hfamap/src/HFAMapImageProbe.mm
          grep -q 'src/HFAMapMenuBinaryAnalyzer.mm' hfamap/Makefile
          grep -q 'com.hfa.menu-callgraph/v2' hfamap/src/HFAMapMenuBinaryAnalyzer.mm
          grep -q 'LC_SYMTAB' hfamap/src/HFAMapMenuBinaryAnalyzer.mm
          grep -q 'SEG_LINKEDIT' hfamap/src/HFAMapMenuBinaryAnalyzer.mm
          grep -q 'objc_msgSend\$' hfamap/src/HFAMapMenuBinaryAnalyzer.mm
          grep -q 'dispatcherSymbols' hfamap/src/HFAMapMenuBinaryAnalyzer.mm
          grep -q 'movz-immediate-candidate' hfamap/src/HFAMapMenuBinaryAnalyzer.mm
          grep -q 'candidate-only-no-abi-binding' hfamap/src/HFAMapMenuBinaryAnalyzer.mm
          grep -q 'directPatchCallEdges' hfamap/src/HFAMapMenuBinaryAnalyzer.mm
          grep -q 'src/HFAIL2CPPRuntimeProbe.mm' hfamap/Makefile
          grep -q 'DobbyHook' hfamap/src/HFAIL2CPPRuntimeProbe.mm
          grep -q 'DobbyDestroy' hfamap/src/HFAIL2CPPRuntimeProbe.mm
          ! grep -q 'DobbyInstrument' hfamap/src/HFAIL2CPPRuntimeProbe.mm
          ! grep -q 'direct-method-entry' hfamap/src/HFAIL2CPPRuntimeProbe.mm
          grep -q 'il2cpp_class_from_name' hfamap/src/HFAIL2CPPRuntimeProbe.mm
          grep -q 'il2cpp_runtime_invoke' hfamap/src/HFAIL2CPPRuntimeProbe.mm
          grep -q 'original ? original(method, object, arguments, exception)' hfamap/src/HFAIL2CPPRuntimeProbe.mm
          grep -q 'kHFAIL2CPPMaxEvents = 256' hfamap/src/HFAIL2CPPRuntimeProbe.mm
          grep -q 'kHFAIL2CPPMaxMappings = 256' hfamap/src/HFAIL2CPPRuntimeProbe.mm

      - name: Install Theos
        run: |
          set -euo pipefail
          brew install ldid
          git clone --recursive --depth 1 https://github.com/theos/theos.git "$RUNNER_TEMP/theos"

      - name: Build arm64 dylib
        env:
          THEOS: ${{ runner.temp }}/theos
        run: |
          set -euo pipefail
          make -C hfamap clean
          make -C hfamap FINALPACKAGE=1
          mkdir -p hfamap/dist/v2.4.1-dev
          cp hfamap/.theos/obj/HFAMapUniversal.dylib \
            hfamap/dist/v2.4.1-dev/HFAMapUniversal-v2.4.1-dev.dylib
          out=hfamap/dist/v2.4.1-dev/HFAMapUniversal-v2.4.1-dev.dylib
          strings_file="$RUNNER_TEMP/hfamap-v2.4.1.strings"
          file "$out" | grep -q arm64
          strings "$out" > "$strings_file"
          grep -q 'com.hfa.il2cpp-runtime-probe/v1' "$strings_file"
          grep -q 'com.hfa.menu-binary-evidence/v2' "$strings_file"
          grep -q 'com.hfa.menu-callgraph/v2' "$strings_file"
          grep -q 'dispatcherSymbols' "$strings_file"
          grep -q 'argumentEvidence' "$strings_file"
          grep -q 'arm64-direct-bl' "$strings_file"
          ! grep -q 'direct-method-entry' "$strings_file"
          shasum -a 256 "$out" | tee hfamap/dist/v2.4.1-dev/SHA256SUMS.txt

      - name: Upload dylib
        uses: actions/upload-artifact@v4
        with:
          name: HFAMapUniversal-v2.4.1-DispatcherPatchArgs
          path: |
            hfamap/dist/v2.4.1-dev/HFAMapUniversal-v2.4.1-dev.dylib
            hfamap/dist/v2.4.1-dev/SHA256SUMS.txt
          if-no-files-found: error
